# ── Docker services ────────────────────────────────────────────────
# PostgreSQL connection used by the API container (set inside docker-compose.yml)
DATABASE_URL="postgresql://jatin:password@localhost:5432/postgres?schema=public"
JWT_SECRET=slooze-production-secret-2026

# ── Next.js frontend (npm run dev) ─────────────────────────────────
# Points to the NestJS API container exposed on host port 3001
NEXT_PUBLIC_API_URL=http://localhost:3001/api/graphql
FRONTEND_URL=http://localhost:3000
